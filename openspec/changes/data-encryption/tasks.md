# Tasks: Data Encryption at Rest (Passphrase Envelope)

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | 950–1150 |
| 400-line budget risk | High (single PR) |
| Chained PRs recommended | Yes |
| Suggested split | PR 1 (crypto.js + crypto.test.js + sw.js) → PR 2 (storage v7 + envelope boundaries + harness) → PR 3 (v6→v7 migration + seeded tests) → PR 4 (boot UX + modals + manual) |
| Delivery strategy | ask-on-risk |
| Chain strategy | pending |

Decision needed before apply: Yes
Chained PRs recommended: Yes
Chain strategy: pending
400-line budget risk: High

Line budget breakdown: crypto.js ≈ 200 (new) + crypto.test.js ≈ 190 (new) + storage.js ≈ +300 (v7/key API ~120, envelope boundaries ~90, migration ~110) + storage.test.js ≈ +180 + app.js ≈ +140 + index.html ≈ +85 + sw.js +1 → ≈ 1050–1100 total across 7 files. PR 1 ≈ 370–420 (sits on the budget line — if it crosses 400, move the DE8/DE9 AAD+portability tests to PR 2); PR 2 ≈ 280–330; PR 3 ≈ 190–240; PR 4 ≈ 210–260. Each slice stays under the 400-line review budget and is independently revertable.

### Suggested Work Units

| Unit | Goal | Likely PR | Focused test command | Runtime harness | Rollback boundary |
|------|------|-----------|----------------------|-----------------|-------------------|
| 1 | Pure crypto envelope layer (`src/crypto.js`) + node-env suite + sw.js precache line | PR 1 | `npx vitest run src/crypto.test.js` | Vitest `// @vitest-environment node` (Node ≥24 global subtle); node REPL: `init('pass') → encryptPayload → decryptPayload` | `src/crypto.js` + `src/crypto.test.js` — revert restores the legacy plaintext boot (storage untouched until PR 2) |
| 2 | storage.js DB v7 + cryptoMeta + key API + envelope-aware boundaries + test harness (subtle inject, initKey beforeEach, ciphertext-at-rest, cross-backend, LS-only) | PR 2 | `npx vitest run src/storage.test.js src/crypto.test.js` | Vitest + fake-indexeddb + injected webcrypto subtle; save/load then inspect raw IDB/LS in DevTools-faithful asserts | `src/storage.js` + `src/storage.test.js` — revert restores v6 plaintext persistence (v7 is additive, orphan cryptoMeta ignored) |
| 3 | v6→v7 migration: `migrateEncryption`, raw accessors, `migrateLsKey`, `migrateLsKeysWhenIdbDown`, seeded legacy suite, purge-after-verify | PR 3 | `npx vitest run src/storage.test.js src/crypto.test.js` | Seeded path: `indexedDB.deleteDatabase` → open(6) → seed plaintext IDB+LS → `initKey` → `load()` → assert v7/ciphertext/lossless | Migration tasks inside `src/storage.js` — revert keeps v6 data intact (additive upgrade; plaintext still recoverable pre-migration-run) |
| 4 | Boot UX: index.html passphrase/change-passphrase modals + #secureContextError panel + app.js cryptoGate + manual checklist | PR 4 | `npx vitest run` (full regression) + manual browser checklist | Browser: `python -m http.server` on localhost:3000; setup/unlock/change flows; DevTools byte-scan of IDB + LS | `index.html` + `app.js` — revert restores the legacy boot path (data caveat: ciphertext is unrecoverable without the passphrase; Excel export is the recovery path per proposal) |

### Carry-over notes (design validation — apply MUST honor these)

- **`migrateLsKeysWhenIdbDown()` — DECISION: IN THIS CHANGE** (tasks 3.5–3.6). It closes the "no plaintext at rest in LS" requirement (DE11/DE12) when IDB is down; deferring would leave a documented plaintext window. Call sites: (1) `load()`'s `openDB()`-failure catch path BEFORE fallback reads, (2) defensively at the end of `migrateEncryption()` (no-op when IDB is up, per design pseudocode). The legacy-plaintext passthrough in `lsLoad*` is TRANSIENT ONLY (pre-migration window); migration purges/rewrites BOTH backends per DE12 (fallback LS keys removed, dual-write keys re-encrypted).
- **Under-defined helpers (name precisely — current storage.js has per-store `idbGetAll*`/`idbPutAll*`, NOT generic raw accessors):**
  1. `rawGetAll(db, storeName)` / `rawPut(db, storeName, record)` / `rawClear(db, storeName)` — generic raw IDB accessors parameterized by store name; bypass the encryption boundary; used ONLY by migration (inspect/write/verify/purge). Plus `storeKeyFor(store)` → `'active'` (aiSettings/settings) else `'__enc__'`.
  2. `migrateLsKey(lsKey, aad)` — rewrites a legacy plaintext LS mirror as a ciphertext envelope with decrypt read-back verify (dual-write mirrors only; fallback keys are purged instead).
  Also spelled out: `idbGetCryptoMeta`/`idbPutCryptoMeta`/`lsLoadCryptoMeta`/`lsSaveCryptoMeta` (record id `'meta'`, LS key `finanzas:crypto-meta:v1`), `hasEncryptedData()` (any raw store row isEnvelope → true; guards `initKey` against meta-missing-but-ciphertext via `KeyMaterialMissingError`), `isEnvelope(v)` (`v && typeof v === 'object' && v.v === 1 && typeof v.alg === 'string' && typeof v.iv === 'string' && typeof v.ct === 'string'`).
- **sw.js**: design lists it "untouched" — true for data flow, but NFR "offline/local operation preserved" requires the NEW asset in the precache list (task 4.1, one line). Without it, offline boot serves storage.js without `window.fpCrypto`.
- PBKDF2 310k iterations (~1 s boot) stays a documented open item needing a low-end-device manual check (design Open Questions).

### Requirement Coverage

| Req | Tasks | Req | Tasks | Req | Tasks |
|-----|-------|-----|-------|-----|-------|
| DE1 | 1.2, 2.1, 4.3 | DE6 | 1.7, 2.2, 4.4 | DE11 | 2.4, 2.7, 2.8, 3.2 |
| DE2 | 1.4, 2.1, 2.3 | DE7 | 2.5, 2.6 | DE12 | 3.1–3.6 |
| DE3 | 1.1, 1.5, 4.3, 4.6 | DE8 | 1.8, 2.4, 2.6 | DE13 | 2.5, 2.6 |
| DE4 | 1.6, 2.1, 4.3, 4.5 | DE9 | 1.9 | DE14 | 2.5, 2.6 |
| DE5 | 1.6, 4.3, 4.6 | DE10 | 4.2, 4.3, 4.4, 4.6 | DE15 | 2.7, 2.8, 3.5, 3.6 |

---

## Phase 1: Crypto Primitives (pure, node-env) — `src/crypto.js` + `src/crypto.test.js`

- [ ] 1.1 RED `src/crypto.test.js` (new): `// @vitest-environment node`; `import * as fpCrypto from './crypto.js'` (collection errors until the file exists = the RED signal) + sanity it "el entorno node expone crypto.subtle" (`expect(globalThis.crypto.subtle).toBeTruthy()` — pins why jsdom files can't host this suite).
- [ ] 1.2 RED DE1 `describe('init() — passphrase policy')`: 'rechaza < 8 caracteres y no deriva nada' — `await expect(fpCrypto.init('corta', null)).rejects.toThrow(/mínimo 8 caracteres/i)` AND `fpCrypto.isEncryptionReady() === false`; 'acepta débil pero válida' — `init('12345678', null)` resolves; 'crea meta con salt fresco' — `meta.salt` is base64(16B)/24 chars, `meta.wrappedDek` base64(48B)/64 chars, `meta.iterations === 310000`.
- [ ] 1.3 GREEN `src/crypto.js`: constants (`ENVELOPE_VERSION=1`, `CIPHER_ALG='AES-GCM-256'`, `KDF_ALG='PBKDF2-SHA256'`, `PBKDF2_ITERATIONS=310000`, `SALT_BYTES=16`, `IV_BYTES=12`, `DEK_BYTES=32`, `KDF_DKLEN=44`); chunked `toB64`/`fromB64` (64 KB chunks — btoa limits); error classes `PassphraseTooShortError`/`WrongPassphraseError`/`SecureContextError`/`EncryptionNotReadyError`/`PayloadAuthError`; `assertSecureContext()`, `init(pass, meta)` (fresh: derive KEK via PBKDF2 dkLen 44 → first 32 KEK + last 12 deterministic wrap IV, generate DEK; meta: unwrap GCM → auth failure = `WrongPassphraseError`), `deriveKek`, `wrapDek`, `buildMeta`, `isEncryptionReady`, `encryptPayload`/`decryptPayload` stubs to land with 1.4; dual export (`window.fpCrypto`, NOT `window.crypto`). Greens 1.1–1.2.
- [ ] 1.4 RED DE2: envelope shape EXACTLY `{v:1, alg:'AES-GCM-256', salt, iv, ct}`; `iv` base64(12B) unique per call; round-trip equality for a small payload AND a >64 KB entries payload (exercises chunked base64); meta contains ONLY wrappedDek + salt + iterations (no 32-byte DEK/KEK anywhere in `JSON.stringify(meta)`).
- [ ] 1.5 RED DE3: `vi.stubGlobal('crypto', undefined)` → `init('valid-pass', null)` rejects `SecureContextError` `/contexto seguro/i` AND `assertSecureContext()` throws; `afterEach(() => vi.unstubAllGlobals())`.
- [ ] 1.6 RED DE4/DE5: helper `freshModule()` = `vi.resetModules()` + `await import('./crypto.js')`; 're-prompt: unwrap correcto tras reset de módulo' (DE4); 'contraseña incorrecta falla limpio' — `init('wrong', meta)` rejects `WrongPassphraseError`, `isEncryptionReady() === false`, no state mutation (subsequent correct init still succeeds — data untouched, DE5).
- [ ] 1.7 RED DE6: capture 2 envelopes → `changePassphrase('cur', 'new-pass-9')` → new meta (salt AND wrappedDek differ) → captured envelopes byte-identical (payload ciphertext NEVER re-encrypted) → `freshModule()` `init(new, newMeta)` decrypts every captured envelope to the original.
- [ ] 1.8 RED DE8: `decryptPayload('entries', env)` succeeds; same env with AAD `'budgets'` and with AAD omitted → `PayloadAuthError` `/almacén incorrecto/i`, no plaintext returned.
- [ ] 1.9 RED DE9: portability = second-device simulation: `freshModule()` (fresh state, no device/browser state) with pure-JSON `capturedMeta` + captured envelope → `init(pass, capturedMeta)` → `decryptPayload` equals the original; assert envelope/meta contain no non-portable fields.
- [ ] 1.10 GREEN exports + gate: `module.exports` = the 6 public fns (+ `toB64`/`fromB64` test surface) and the `window.fpCrypto` block with identical surface; parity it; Gate: `npx vitest run src/crypto.test.js` green.

## Phase 2: Storage Envelope Integration + Key API — `src/storage.js` + `src/storage.test.js`

- [ ] 2.1 RED harness + DB v7 + cryptoMeta-at-rest: at test top `import { webcrypto } from 'node:crypto'; globalThis.crypto.subtle = webcrypto.subtle;` (jsdom 29 exposes only getRandomValues/randomUUID); `beforeEach`: `await storage.initKey('contraseña-de-prueba-2026'); await storage.clear();` (gate first, then the existing clear — do NOT clear before key resolution). New its: opens DB v7 with `cryptoMeta` + the 6 legacy stores; `hasEncryptionKey()` true after initKey + initKey idempotent with same pass; raw IDB `cryptoMeta` record id `'meta'` + raw LS `finanzas:crypto-meta:v1` hold ONLY wrappedDek + salt (no kek/dek plaintext). All existing tests error on the missing `initKey` export = RED.
- [ ] 2.2 GREEN `src/storage.js`: `DB_VERSION` 6→7 (line 9) + version comment; `cryptoMeta` store creation in `opupgradeneeded` (after settings block, lines 57–59, guarded `contains()`); constants `CRYPTO_META_STORE`, `LS_CRYPTO_META_KEY='finanzas:crypto-meta:v1'`, `ENC_KEY='__enc__'`; `fpCrypto` resolution (`window.fpCrypto` → `require('./crypto.js')` for node); `isEnvelope(v)`; `assertKeyReady()` throwing 'Cifrado no inicializado: llamá storage.initKey(passphrase) primero.'; `idbGetCryptoMeta`/`idbPutCryptoMeta`/`lsLoadCryptoMeta`/`lsSaveCryptoMeta`; `hasEncryptedData()`; `initKey(pass)` (meta = IDB || LS; missing + hasEncryptedData → `KeyMaterialMissingError`; `fpCrypto.init`; dual-write meta to IDB + LS), `changePassphrase(cur, next)`, `hasEncryptionKey()`; export the 3 new fns in `module.exports` (lines 644–645) + `window.storage` (line 649). Greens 2.1.
- [ ] 2.3 RED DE1/DE2-at-storage + gate-before-write: fresh module WITHOUT initKey → `save()` and `load()` reject `/no inicializado/i` and `idbClear`/`rawPut` spies are NOT called (no partial writes before key resolution); with initKey → `save(sampleEntries)` → raw `entries` store = exactly ONE envelope record (isEnvelope, id `'__enc__'`) and `load()` deep-equals sampleEntries (tipo normalization intact).
- [ ] 2.4 GREEN entries boundary: `idbGetAll` (1 row + isEnvelope → `fpCrypto.decryptPayload('entries', rec)`; else legacy passthrough), `idbPutAll` (`assertKeyReady()` + `{ id: ENC_KEY, ...await fpCrypto.encryptPayload('entries', payload) }` — whole-payload, AAD = canonical store name, identical in both backends per DE11), `load()` order: `assertKeyReady` → `openDB` → `migrateFromLS` → (migrateEncryption hook lands in 3.2) → `idbGetAll` → normalize `tipo || 'expense'`; `save()`: `assertKeyReady` → `idbClear` → `idbPutAll`. Greens 2.3.
- [ ] 2.5 RED DE7/DE13/DE14: 'apiKey nunca en claro' — `saveAiSettings({provider, apiKey:'sk-test-123', model})` → raw IDB record AND raw LS JSON: isEnvelope true + byte-scan `!JSON.stringify(raw).includes('sk-test-123')`; 'dual-write cifrado en ambos lados' — `saveCurrencySettings({...})` → raw `settings` store + raw LS `finanzas:settings:v1` both envelopes, `loadCurrencySettings()` returns the plaintext record (DE13); 'customCategories cifrado y sin pérdida' — `saveCustomCategories(cats)` → raw store + raw LS envelopes, `loadCustomCategories` deep-equal (DE14).
- [ ] 2.6 GREEN remaining boundaries: envelope-aware `idbGetAiSettings`/`idbPutAiSettings` (`get('active')` → isEnvelope ? decryptPayload('aiSettings', rec) : rec; `put({ id:'active', ...env })` — same-key upsert atomically replaces v6 plaintext) and identical for `idbGetSettings`/`idbPutSettings` ('settings'); envelope-aware `lsLoad*`/`lsSave*` for all 6 keys (`lsSaveX` = JSON.stringify of `{ id: <'active'|'__enc__'>, ...env }`; `lsLoadX` = isEnvelope ? decrypt : legacy passthrough); all public `load*/save*` (entries, budgets, recurring, customCategories, aiSettings, currencySettings) routed through encrypted boundaries with plaintext in-memory objects. Greens 2.5.
- [ ] 2.7 RED DE11/DE15 + dark-mode: 'cifrado idéntico en IDB y LS — cualquiera descifra' — save → raw IDB envelope deep-equal raw LS envelope (same salt/iv/ct) → `globalThis.indexedDB = undefined` (restore in afterEach) → load() decrypts the LS mirror to identical plaintext; 'round-trip LS puro (IDB caído)' — IDB down → `save(sampleEntries)` → LS key is an envelope (no plaintext) → load() → exact original (DE15); 'passthrough legacy LS transitorio' — raw legacy plaintext array in LS + IDB down → load() returns it as-is (documented pre-migration window); 'finanzas:dark-mode sigue en claro' — raw LS value untouched by the save/load cycle.
- [ ] 2.8 GREEN LS-fallback glue: `save()` catch path → envelope-aware `lsSave`; `load()` catch (openDB failure) → envelope-aware `lsLoad` + legacy passthrough; `loadAiSettings`/`loadCurrencySettings` LS-mirror paths decrypt envelopes (mirrors live even when IDB works — dual-write). Greens 2.7. Gate: `npx vitest run src/storage.test.js src/crypto.test.js`.

## Phase 3: Migration v6→v7 (seeded legacy suite)

- [ ] 3.1 RED DE12 (lossless): helper `seedLegacyV6()` — `indexedDB.deleteDatabase(DB_NAME)` → `indexedDB.open(DB_NAME, 6)` → create the 6 legacy stores with their keyPaths → seed plaintext rows in ALL stores (entries ×2, budgets, recurring, customCategories, aiSettings 'active' WITH apiKey, settings 'active') + plaintext LS keys for all 6 incl. dual mirrors → `storage.initKey(PASS)` → `storage.load()`; asserts: `db.version === 7`; cryptoMeta store + meta record present; every raw store = exactly one envelope; dual LS keys are envelopes; fallback LS keys (`gastos`, `budgets`, `recurring`, `custom-categories`) REMOVED; `load()` deep-equals seeded plaintext (zero loss).
- [ ] 3.2 GREEN `migrateEncryption(db)`: `MIGRATION_PLAN` 6 rows `{ store, lsKey, aad (canonical store name), mode: 'fallback'|'dual' }` — entries/gastos/fallback, budgets/fallback, recurring/fallback, customCategories/fallback, aiSettings/dual, settings/dual; raw accessors `rawGetAll(db, storeName)`/`rawPut(db, storeName, record)`/`rawClear(db, storeName)` + `storeKeyFor(store)` ('active'|'__enc__') + `migrateLsKey(lsKey, aad)` (under-defined helpers #1–#2, design carry-over); per store: skip when empty/already-envelope (dual → still `migrateLsKey` the mirror), else assertKeyReady → encrypt whole payload → `rawPut` → (verify lands 3.4) → mode dual ? `migrateLsKey` : `localStorage.removeItem(lsKey)`; `load()` calls `migrateEncryption(db)` right after `migrateFromLS`. Greens 3.1.
- [ ] 3.3 RED DE12 (purge-after-verify): `vi.spyOn(window.fpCrypto, 'encryptPayload').mockRejectedValueOnce(new Error('boom'))` → `load()` REJECTS with a migration error (NOT "starts empty"), raw store rows still legacy plaintext (`rawClear` NOT called), plaintext LS keys retained; unspy → retry succeeds and purges.
- [ ] 3.4 GREEN verify-before-purge: after `rawPut` ciphertext → `rawGetAll` read-back → `JSON.stringify(back) === JSON.stringify([env])` deep-equal → ONLY then `rawClear` + final `rawPut`; on mismatch throw `MigrationVerifyError` BEFORE any purge (plaintext retained, next boot retries — error-handling table). Greens 3.3.
- [ ] 3.5 RED DE12-LS + key-aware migrateFromLS: (a) IDB unavailable (`globalThis.indexedDB = undefined`) + legacy plaintext LS (v6 leftovers) + initKey → load() LS path → EVERY LS key is now a ciphertext envelope (encrypt-in-place, decrypt-verified) and load() returns the plaintext data; (b) LS-only v6 (finanzas:migrated absent, IDB empty) → load() → entries migrated to IDB ciphertext, `lsClear()` only after decrypt-verify, migrated flag set.
- [ ] 3.6 GREEN `migrateLsKeysWhenIdbDown()` (DECISION: in scope): for each of the 6 LS keys — raw non-envelope → `assertKeyReady` → encrypt-in-place via same-key `localStorage.setItem` (atomic replace; read-back decrypt verify); call sites: (1) `load()`'s openDB-failure catch BEFORE fallback reads, (2) end of `migrateEncryption()` (defensive no-op when IDB up); `migrateFromLS` gains decrypt-verify before `lsClear()` (`finanzas:migrated` behavior unchanged). Greens 3.5. Gate: `npx vitest run`.

## Phase 4: Boot Gating + Modals — `index.html`, `app.js`, `sw.js`

- [ ] 4.1 `index.html` + `sw.js`: insert `<script src="src/crypto.js"></script>` between finance.js (line 516) and storage.js (line 517 — load order is the contract); sw.js precache list (lines 14–15) gains `'/src/crypto.js'` (offline NFR, design carry-over). Red: none (markup) — 4.6 manual + grep checks.
- [ ] 4.2 `index.html` markup: `#passphraseModal` after `#currencySettingsModal` (~line 511) with `data-bs-backdrop="static" data-bs-keyboard="false"` (boot cannot proceed without it); setup mode: `#newPassphrase` + `#confirmPassphrase` + inline `#passphraseError` + `#btnSetPassphrase` + DE10 warning "Si perdés la contraseña, tus datos son irrecuperables. Exportá regularmente a Excel (📥 Exportar) como respaldo."; `#changePassphraseModal`: `#changeCurrentPassphrase`/`#changeNewPassphrase`/`#changeConfirmPassphrase`/`#changePassphraseError`/`#btnChangePassphrase` + DE10 warning; navbar `🔐 Clave` (`id="btnPassphrase"` after btnCurrencySettings line 26 → data-bs-target `#changePassphraseModal`); `#secureContextError` block panel (covers app content). Red: none (markup) — 4.6 manual.
- [ ] 4.3 `app.js` `cryptoGate()`: (1) `window.fpCrypto.assertSecureContext()` catch → show `#secureContextError`, return false — boot refuses, no loads, NO silent plaintext (DE3); (2) `storage.hasEncryptionKey()` → open `#passphraseModal` in setup (no key: pass + confirm + warning) or unlock (key: pass only, no confirm); (3) submit → `await storage.initKey(pass)`; `WrongPassphraseError` → inline "Contraseña incorrecta", modal stays open, data untouched (DE5); `PassphraseTooShortError` → inline min-length error; success → hide modal; setup-mode → one-time DE10 recovery toast (plaintext flag `finanzas:recovery-warned` = metadata, out of scope) — return true.
- [ ] 4.4 `app.js` modal handlers: `setupPassphraseModal()` (Enter submits; backdrop/keyboard disabled by 4.2 markup) + `setupChangePassphraseModal()` mirroring `setupCurrencySettingsModal()` (line 373): validate new ≥8 + confirm match → `await storage.changePassphrase(cur, next)` → inline `WrongPassphraseError`/too-short errors, success toast "Contraseña actualizada", payloads never re-encrypted (DE6 — proven at unit level by 1.7).
- [ ] 4.5 `app.js` `init()` (line 1655): insert `if (!(await cryptoGate())) return;` immediately before `await loadFromStorage()` (1656) — `checkAndGenerateRecurring()` (1662) then runs only post-key (no plaintext write at boot); call `setupPassphraseModal()` + `setupChangePassphraseModal()` beside `setupAiSettingsModal()` (1680) / `setupCurrencySettingsModal()` (1681).
- [ ] 4.6 Manual browser checklist (final PR gate; `python -m http.server` on localhost:3000): first boot → setup modal with DE10 warning; weak-but-valid accepted; every reload → re-prompt (DE4); wrong passphrase → inline error, data untouched, retry succeeds (DE5); change passphrase → reload decrypts with the new passphrase, payload bytes unchanged in DevTools (DE6); DevTools byte-scan IDB + LS → envelopes only, zero plaintext amounts/apiKey; the ONLY plaintext left: `finanzas:dark-mode`, `finanzas:migrated`, `finanzas:recovery-warned` (DE7/DE11/DE12); Excel export works (recovery path, DE10); dark-mode toggle still instant; DevTools offline → boot + decrypt from SW cache (crypto.js precached per 4.1); Safari `file://` / LAN HTTP → `#secureContextError` panel and boot refuses (DE3 — stub-check if no such browser available, and document).

## Phase 5: Finalize + Full Suite

- [ ] 5.1 Consolidate suites: `crypto.test.js` describes ordered DE1→DE9; `storage.test.js` grouped ciphertext-at-rest / cross-backend / migration; every new assert EXACT-VALUE (byte-identical), no regex-loose; stubs restored (`afterEach`: `vi.unstubAllGlobals` / `vi.restoreAllMocks`).
- [ ] 5.2 Gate: `npx vitest run` full green — all existing tests stay green with harness-only changes (subtle inject + `initKey` in beforeEach; assertions untouched) plus the new crypto/storage suites.

## Definition of Done

- [ ] All 5 phases complete, every checkbox checked
- [ ] Every requirement DE1–DE15 covered by at least one task (see Requirement Coverage table)
- [ ] `npx vitest run` green: legacy assertions unchanged + new crypto/storage suites
- [ ] Slices landed as conventional commits, no AI attribution, each slice independently revertable
- [ ] Manual checklist (4.6) verified in browser: boot gate, re-prompt, wrong-passphrase retry, change re-wrap, byte-scan, offline, secure-context refusal